# SoDam-Design-Kit -- 디자인 문서

> Show Me The PRD로 생성됨 (2026-07-19)
> 원천 자료 정본: [RESEARCH_SOURCES_LIST.md](./RESEARCH_SOURCES_LIST.md) → [-01](./RESEARCH_SOURCES-01.md)(코드·검증 자동화) + [-02](./RESEARCH_SOURCES-02.md)(마케팅 소재 자동화) + [-03](./RESEARCH_SOURCES-03.md)(법률·라이선스 자동화)
> ※ 구본 RESEARCH_SOURCES.md(-01과 동일 사본)는 사용자가 정리 완료 — 2026-07-19 폴더 실측으로 부재 확인. 정본은 위 LIST 3종 체계

**한 줄 요약**: Figma 디자인을 읽어 shadcn/ui 코드로 만들고, 실제 브라우저 검증을 통과해야만 "완료"가 되는 Claude Code 디자인 자동화 킷. (Phase 3에서 마케팅 소재 자동화로 확장)

## 문서 구성

| 문서 | 내용 | 언제 읽나 |
|------|------|----------|
| [01_PRD.md](./01_PRD.md) | 뭘 만드는지, 차별점, UI/UX 기준(§5), 보안 기준(§6), 법률·라이선스 기준(§7), 문서화 기준(§8), 성공 기준 | 프로젝트 시작 전 |
| [02_DATA_MODEL.md](./02_DATA_MODEL.md) | .design-kit/ 파일 구조 (DB 없음) | 파일 설계할 때 |
| [03_PHASES.md](./03_PHASES.md) | 3단계 계획 + Phase 1 시작 프롬프트 | 개발 순서 정할 때 |
| [04_PROJECT_SPEC.md](./04_PROJECT_SPEC.md) | 기술 스택 + AI 금지/필수 규칙 | AI에게 코드 시킬 때마다 |

## 다음 단계

Phase 1을 시작하려면 [03_PHASES.md](./03_PHASES.md)의 "Phase 1 시작 프롬프트"를 새 세션에 복사해 사용하세요.

## 미결 사항 종합 (각 항목에 추천안 명시됨)

| # | 항목 | 강력 추천안 | 위치 |
|---|------|-------------|------|
| 1 | 플러그인 이름 | **SoDam-Design-Kit 확정** (2026-07-19 사용자 답변, id: `sodam-design-kit`) | 01_PRD |
| 2 | Figma 연결 경로 | **완전 해소(2026-07-20, 20차)** — 3경로(원격 MCP 권장·커넥터·데스크톱 MCP)+4클라이언트 카탈로그 확인·로컬 .fig 불가 함정 기록 + 읽기 깊이 스파이크 실측 PASS(실제 Figma 데이터로 매핑·생성 충분성 확인). 잔여 없음 | 01_PRD §11 |
| 3 | 베타 공개 시점 | Phase 3에서 사용자 결정 (착수는 막지 않음) | 01_PRD |
| 4 | 킷 자체 라이선스 (2026-07-19 신규) | 현재 미지정(확인됨) — 공개 결정 시 Apache-2.0 추천(형제 관례), 저작권자 표기 포함 사용자+법무 확정. 확정 전=본인 사용만 | 01_PRD §7 |

> (구 번호 기준) 스크린샷 보관·훅 강제 수준·axe 실행 방식은 2026-07-19 재감사에서 **확정**되어 미결에서 제외 — 02·04 문서의 확정 표기 참조. 1(이름)은 **SoDam-Design-Kit**로 확정(6차). 2(Figma 연결 경로)는 **20차에 완전 해소**(2026-07-20 정정 — 이전 판은 17차 시점 "잔여=읽기 깊이 스파이크" 상태가 20차 완전 해소 이후에도 이 표에만 갱신되지 않고 남아있던 모순이었음). **남은 미결 2건 = 3(공개 시점)·4(킷 라이선스, 14차 신규)** — 둘 다 사용자 결정 몫이라 문서 감사로는 해소 불가.

## 재감사 반영 이력 (2026-07-19)

- **모순 1건 정정**: Lighthouse를 Node 스크립트에서 MCP로 호출하는 불가능한 조합 → 게이트 판정은 @axe-core/playwright 단일화, Lighthouse는 참고 지표
- **누락 4건 보완**: dev server 자동 기동(포트 자동 우회) / component-map 초기 시드 / 기설치 디자인 플러그인 역할 경계 절 / 원천 자료 링크 실측 체크
- **추측 2건 실측 전환**: Figma 커넥터 읽기 깊이 → Phase 1 첫 작업 스파이크 / 훅 오탐 → runs/ 상태 파일 조건부 발동
- **3차(같은 날)**: P1 프리뷰 라우트 신설(컴포넌트 단위 검증 공백 해소) / 실패 피드백 주입 / PASS 조건 4종 단일 출처 명문화(02) / shadcn 미설치 init 분기 / 개인정보 Figma 연결 금지 규칙 / 킷 자체 단위 테스트를 P1 완료 조건에 추가
- **4차(같은 날, RESEARCH_SOURCES-01.md 정독 포함)**: P1 Next.js 전용 고정(스코프 크립 차단) / FAIL 2연속 확정 재검 규칙(flaky 오탐 분리) / 시각 회귀 기준본 baseline/ 분리(보관 정책 충돌 해소) / 전 스크립트 Node .mjs 단일화(Windows 함정 예방). ※ RESEARCH_SOURCES-01.md는 정독 결과 기존 RESEARCH_SOURCES.md와 내용 동일 사본(확인됨) — 신규 반영 항목 없음
- **5차(같은 날, RESEARCH_SOURCES-02.md·LIST 정독)**: -02는 신규 내용(마케팅 소재 자동화 8개 영역) — Phase 3를 "소재+상세페이지"로 재정의(P1/P2 불변) / 소재 1차 경로 Satori+Sharp 무료·로컬 확정, Canva Autofill은 Enterprise 조건으로 제외 / 소재 전용 검증 기준 신설 / shadcn Registry MCP를 P2 스파이크로 추가 / AI 로고 상표·저작권 검토 규칙 승격 / 원천 자료 정본을 LIST(-01·-02)로 확정
- **6차(같은 날)**: 이름 확정 **SoDam-Design-Kit**(사용자 답변) + 구본 보관 확정 / Satori 한글 OFL 폰트 단계 명시(폰트 명시 로드 필수 — 누락 시 한글 렌더 실패) / 명함을 소재 파이프라인에 추가, 로고는 유료 어댑터 전용 명시 / 레이아웃·랜딩=P1 담당 표기(중복 구현 차단) / 01·README 개요에 P3 확장 부기. 발견 강도 5라운드 연속 하락(치명 1→1→0→0→0) — 문서 감사 수렴, 다음은 Phase 1 실측
- **7차(같은 날, RESEARCH_SOURCES-03.md 정독 — 신규: 법률·라이선스 자동화)**: P3 라이선스 게이트(자산 대장 3종 — -03 §23의 15종을 개인 규모로 압축) / AI 생성 이력 자동 기록 / ScanCode는 공개 시점 전용 / WIPO 등 IP DB 브라우저 자동 조회 금지 DO NOT(약관 위반 예방) / KIPRISPlus·C2PA 백로그 / 정본에 -03 추가 / **폰트 파이프라인 신설: A 전자동(OFL 다운로드→라이선스 보존→대장→next/font)·B 선택 반자동(후보 3종 스펙시트→1회 선택)·C 수동 게이트(미등록 폰트 FAIL·로고 워드마크 수동 확인)** — P2 배치, P3 Satori 재사용
- **8차(같은 날, 자료 변화 없음 — 내부 정합성 감사)**: ASSET-LEDGER.csv 생성 시점을 P2로 이동(P2가 P3 파일에 기록하려는 순서 결함 해소) / 폰트 게이트 "P2부터 적용" 표기(P1 스코프 보존) / P2 완료 기준 확장(폰트 게이트 실FAIL 사례 1건)·기간 4~6세션 조정 / P3 전제에 P1·P2 게이트 재실행 추가. 치명 0 — 신규 자료 없는 라운드는 정합성 수준만 남음, 실측 전환 재권고
- **9차(같은 날, 자료 무변경 타임스탬프 실측)**: 구본 삭제 반영(사용자 직접 정리 — 폴더 실측 부재 확인) / 02 파일 트리에 P2·P3 파일 반영(그림-본문 동기화) / VerifyReport 표에 recheck 행 추가 / 01 기능 표에 폰트 파이프라인·라이선스 게이트 2행 추가. 전 항목 동기화 수준 — **문서 감사 최종 종결, 다음 = 새 세션 Phase 1 실측**
- **10차(같은 날, 종결 검증 라운드)**: 자료 무변경 2회 연속 실측 확인 / "가칭" 잔재 0건(이름 교체 무결) / 삭제된 구본을 가리키던 낡은 참조 2건(01 표지 메타·해결하는 문제 첫 문장)을 -01~-03 체계로 갱신. **검증 통과 — PRD 확정, 감사 루프 닫힘(새 자료 추가 시에만 조건부 재개)**
- **11차(같은 날, 사용자 신규 결정 2건 — 조건부 재개)**: 폴더 개명(`26y_07m_26d_` → `26y_07m_26d_SoDam-Design-Kit`) 실측 반영 — 문서 내 옛 경로 잔존 0건 확인(전부 상대 경로라 무사) / **open 대시보드** 편입(P2, 읽기 전용, O-Brain server.mjs 보안 패턴 이식 — 127.0.0.1 전용·실행별 토큰·CSP·포트 자동 우회) / **멀티 클라이언트 MCP 래퍼** 편입(P3, Claude Desktop .mcpb 1순위 확인됨 / GPT Desktop은 원격 HTTPS 전용 실측이라 조건부 백로그 / **완료 차단은 Claude Code 훅 전용** — 01에 능력 매트릭스 신설) / DO NOT 2건 추가(대시보드 상태 변경 금지·터널 외부 노출 금지) / 01 "자체 MCP 서버 안 함"을 P1 한정으로 축소(변경 사유: 사용자 결정 + O-Brain 선례)
- **12차(같은 날, UI/UX 요구사항 보강)**: 01에 §5 UI/UX 요구사항 신설(이후 섹션 6~8 재번호) — 적용 대상 2종 구분(생성 코드 / 킷 자체 화면), 디자인 방향(심플·모던·실서비스 완성도, "AI식 디자인" 배제), MoSCoW 우선순위(Must=AA 대비·상태 4종·폼 라벨·터치 44px·토큰 재사용 등), 상태 피드백 9종(hover~warning), 로직 불변경 제약(표시 코드만 수정·재시도 중 로직 변경 필요 시 중단·보고). **게이트는 불변**(PASS 조건 4종 유지 — 대비·라벨은 axe가 이미 검사, 나머지는 04 생성 규칙으로 강제). 04 DO NOT 4건·ALWAYS DO 3건 추가, 03 대시보드 화면 기준 연결
- **13차(같은 날, 보안 요구사항 보강 — Secure by Design)**: 01에 §6 보안 요구사항 신설(이후 섹션 7~9 재번호) — 신뢰 경계 표(Figma 데이터·config·로컬 서버·폰트 다운로드 전부 "불신" 전제), MoSCoW(Must=시크릿 Git 제외·spawn 인자 배열만·경로 정규화·대시보드 이스케이프·127.0.0.1+randomBytes 토큰·npm audit high+), **해당 없음 명시**(로그인/세션/JWT/DB/SQL/결제 — 없는 것을 없다고 + 서비스화 시 ASVS 재작성을 착수 게이트로 부활 조건 명시). 04 보안 DO NOT 4건·ALWAYS DO 3건 추가, 02 파일 트리에 .api-token(0600·gitignore), 03 P2 보안 실측 3건·P3 시크릿 스캔+secret scanning/Dependabot. **게이트·기존 로직 불변**
- **14차(같은 날, 법률·라이선스·상업적 용도 보강)**: 01에 §7 신설(이후 섹션 8~10 재번호) — **킷 자체 라이선스 미지정 사실 명시**(확정 전=본인 사용만·제3자 배포 금지, 공개 시 Apache-2.0 추천은 형제 관례 근거·결정은 사용자+법무), 자산·구성요소별 권리 관계 표(의존성 실측 확인 의무·MPL 무수정 원칙·OFL 단독 재판매 금지·AI 생성물 사용 전 확인), README 안내 요구(한/영 일관·사용 범위 O/X 표·책임 제한·보증 없음·왕초보 눈높이), MoSCoW, **법무 검토 필요 5항 구분**(자동 확정 불가 명시). 03 P3에 라이선스 확정·산출물(LICENSE·NOTICE·README 반영) 항목, 04 DO NOT 2건(타인 상표·개인정보 예제 금지 / 확정 전 배포 금지)+의존성 라이선스 실측 규칙+배포 1단계에 LICENSE 게이트, 미결 표에 #4(킷 라이선스) 추가. **기존 자산 대장 체계(-03 편입분)와 중복 없이 "킷 자체 권리"만 신설** — 게이트·기존 로직 불변
- **16차(같은 날, §5~§8 신설 후 첫 정합성 검사 — 자료 무변경 실측)**: 자료 4종 타임스탬프 무변경 확인. 발견 2건(치명 0) 반영 — ① 미결 표 주석이 신규 #4(라이선스)를 누락한 모순 갱신 ② 01 §9 성공 기준에 최소 문서 세트 1행 추가("구현 완료 정의에 문서 포함"과 §9의 어긋남 해소). 04→01 교차 참조(§5~§8)·§10→§3·02 .api-token 정합은 검증 통과. **감사 루프 재종결 — 발견 강도 치명→구조→표기→주석 한 줄로 단조 하락(수렴), 다음 = 새 세션 Phase 1 실측(Figma 스파이크 30분)**
- **15차(같은 날, 문서화 요구사항 보강)**: 01에 §8 문서화 요구사항 신설(이후 섹션 9~11 재번호) — "구현 완료" 정의에 문서 포함(P1 최소 세트·P3 전체 세트), **대상 독자=IT 기기를 처음 다루는 왕초보**(복사-붙여넣기 명령·클릭 단위 순서·용어 한 줄 풀이·"이 화면이 보이면 성공" 판정), 필수 목차 11종(설치·준비물·빠른 시작·사용법·명령어·워크플로우·보안/데이터 흐름·아키텍처·파일 위치·트러블슈팅·FAQ·법률), **트러블슈팅 최소 세트는 실측 함정 기반**(완전 재시작 캐시·게이트 차단=정상 동작·포트 충돌·한글 폰트 등 8종), 문서만 보고 따라 하는 **새 세션 설치 리허설 1회**를 공개 Must로. 03 P1 최소 문서·P3 전체 세트 배치, 04 문서 동기 갱신 규칙(드리프트 방지)
- **17차(2026-07-20, Figma 연결 경로 실측 — 신규 외부 실측 반영)**: 공식 MCP 카탈로그·공식 설정 가이드 실측으로 연결 경로 확정 — 04 기술 스택을 3경로 체계로 교체(원격 MCP `mcp.figma.com/mcp` 권장·전 플랜 / claude.ai 커넥터 기설치 / 데스크톱 MCP 선택 기반), Claude Code 권장 경로=공식 플러그인 `figma@claude-plugins-official`(**-01 §2가 이미 담고 있던 링크를 PRD 본문이 2단계로 축약했던 갭 해소**) / **로컬 .fig 직접 읽기 불가 함정 실측**(계정 저장 필수) — 01 §8 트러블슈팅·03 인증 절 반영 / 01 능력 매트릭스에 "Figma 읽기는 4클라이언트 공통, 킷 차별점은 게이트" 보충 + ChatGPT 로컬 표기 재확인 플래그(P3) / 스파이크 범위 "경로 확정"→"읽기 깊이"로 축소(01 §11·03 P1)
- **18차(2026-07-20, 멀티 표면 등가성 보강 — 사용자 요청 "어디서 써도 동일 기능")**: 01 §3에 **기능×표면 Parity 매트릭스 신설**(T1 코어=표면 무관 100% 동일 / T2 AI 작업=AI 클라이언트만 / T3 완료 차단=CC 훅 유일 — 되지 않는 것을 동일하다 표기하지 않는 정직 원칙) / **대시보드 정밀 격상**: 읽기 전용 → 열람+T1 재검증 트리거(상태 파일 직접 쓰기는 계속 금지 — "트리거≠쓰기" 구분으로 11차 게이트 우회 차단 정신 유지, 01·02·03·04 동기 수정) / 04에 **연결/동기화 스펙 신설**(단일 출처 `.design-kit/` · `.lock` 실행 1개 원칙 신규 — 동시 실행 시 runs/ 순번·포트 충돌 위험 해소 · 버전 단일화 · 설치 3형태 확인 기준) / GPT Desktop 조건부 승격 규칙 명문화(P3 재실측 조건). P1 스코프·게이트 불변
- **19차(2026-07-20, Figma 요금 한도 실측 확정 — whoami + 공식 rate-limits 문서)**: 위험 2(무료 플랜 가용성)를 URL 없이 실측 해소 — **원격 MCP는 무료 Starter에서도 읽기 가능(핵심 가치 "무료" 유지)이나 Starter/View 시트는 읽기 월 6회 하드 리밋**, Dev/Full 유료는 200~600/일. **사용자 실측 = Starter/View = 월 6회**. 데스크톱 MCP는 Dev/Full 유료 전용으로 확정(17차 "무료 가용성 실측 필요" 해소). 함의: **component-map 캐시·재사용을 "품질 향상"→"생존 필수"로 격상**(04 ALWAYS DO 신규 규칙) / 스파이크는 월 6회 예산 안에서 실행(03 P1) / 01 §11·04 기술 스택 동기 수정. 부족 시 요구사항 기반 생성이 무료 대체 경로 후보(위험 3 — 스파이크 후 결정). P1 스코프·게이트 불변
- **20차(2026-07-20, Phase 1 첫 작업 "Figma 읽기 깊이 스파이크" 실측 PASS — 문서 감사 아닌 실제 실행 결과)**: 실제 Figma Community 파일(10페이지)의 Wireframe 페이지를 실제 MCP 호출로 읽어 **매핑·생성에 충분한 데이터 깊이를 확인**(시맨틱 레이어명·명명된 디자인 토큰·정확한 좌표/색상 코드·노드ID 임베드·스크린샷). 총 4/6회 소모. 미결 #2(Figma 연결 경로) **완전 해소** — 남은 미결은 #3(공개 시점)·#4(킷 라이선스) 2건뿐(둘 다 사용자 결정 몫, 문서 감사로 해소 불가). **부수 발견 2건을 04 ALWAYS DO에 신규 규칙으로 승격**: ① 파일 전체 열거는 무료 3페이지 제한에 걸림 → 항상 페이지/노드 직접 링크 사용(우회 확인됨) ② 이미지·SVG 자산 URL 7일 만료 → 즉시 로컬 다운로드 필수. 03 P1 스파이크 항목 [x] 완료 처리. P1 스코프·게이트 불변, 다음은 실제 파이프라인 구현 착수
- **21차(2026-07-20, Phase 1 실제 구현+반복 라이브 테스트 — 문서 감사가 아닌 실제 실행·재현·수정 결과)**: `pipeline-codegen.mjs`(재사용 경로) 구현 + 실제 Figma 데이터(Button, 421:3078)로 왕복 PASS 실증. 이후 여러 라운드의 라이브 테스트(정상/예외/경계값/실패 상황 실측)에서 **진짜 결함 3건을 재현→원인분석→수정→재검증**: ① `package.json`의 `playwright`·`@axe-core/playwright`가 `devDependencies`로 잘못 분류돼 있어 새 설치 시 누락 위험 → `dependencies`로 정정 + README 설치 절차에 `npm install` 단계 추가 ② `hooks/verify-gate.mjs`가 판정 기록 손상 시 조용히 통과(fail-open)시키던 것을 차단(fail-closed)으로 수정(02 결정 기록 추가) ③ `scripts/setup-wizard.mjs --force`가 이미 확보한 Figma 매핑을 통째로 초기화하던 데이터 유실 버그를 병합·보존 로직으로 수정(02 결정 기록 추가). `tests/report-writer.test.mjs` 신설(기존 커버리지 0 → 4개)로 테스트 공백 해소, 04 "테스트 방법"의 존재하지 않는 `/design-kit selftest` 참조 정정. 전 과정 실제 브라우저 E2E(PASS/FAIL/재검증 3케이스) + 유닛테스트(41/41) + 민감정보 스캔(0건)으로 회귀 없음 재확인. **P1 코드·테스트·문서(02·04·README·commands/setup.md) 전부 동기화 완료** — 남은 것은 사용자만 실행 가능한 새 세션 marketplace 설치 재현 1건뿐
- **22차(2026-07-20, 21차 이후 추가 라이브 테스트 + 우선순위 제안·실행 — 문서 감사가 아닌 실제 실행·재현·수정 결과)**: 21차 이후 이어진 테스트 라운드에서 **④번째 진짜 결함**을 추가로 재현→수정→재검증 — `setup-wizard.mjs --project`가 존재하지 않는 프로젝트 경로에도 `pipeline-codegen.mjs`·`preview-route.mjs`와 달리 조용히 "성공"(exit 0) 처리하며 엉뚱한 위치에 빈 `.design-kit/`를 생성하던 불일치를 `existsSync(projectDir)` 가드로 통일(02 결정 기록 추가). 이어서 "PRD엔 있는데 코드가 없는 항목"을 점검하는 제안-진행 라운드에서 **02가 이미 "확정"으로 체크해둔 스크린샷 10회 보관 정책이 실제로는 전혀 구현되지 않아 픽스처에 16개까지 무제한 누적되고 있었고, `.gitignore` 미등록으로 `.design-kit/` 전체가 git에 untracked 방치**되던 것을 실측 확인 → `report-writer.mjs`에 `pruneScreenshots()`, `setup-wizard.mjs`에 `ensureScreenshotsGitignored()`(멱등 skip 경로에서도 자가 치유)를 구현해 실제 픽스처에서 16개→10개 정리 + `.gitignore` 등록까지 실측 확인(02 결정 기록 추가). 전 과정 유닛테스트(43/43)와 실제 E2E 재실행(PASS/FAIL/재검증 3케이스, 스크린샷 정리 포함)으로 회귀 없음 재확인. **P1 코드·테스트·문서(02·README) 전부 동기화 완료** — 남은 것은 21차와 동일하게 사용자만 실행 가능한 새 세션 marketplace 설치 재현 1건뿐
- **23차(2026-07-20, 사용자의 실제 새 세션(D:\Test_Dev\test11) 라이브 설치 시도 — 문서 감사가 아닌 실제 재현 결과)**: 사용자가 실제로 별도 세션에서 `/plugin marketplace add` → `/plugin install sodam-design-kit@sodam`을 실행해 **설치 자체는 성공**(`✓ Installed`)했으나, 그 다음 안내를 따라가다 **README.md §설치 4번이 실측과 다른 경로를 안내하는 결함**을 실제로 발견 — 이 플러그인은 `"source": "directory"`(로컬 폴더 직접 참조) 마켓플레이스라 설치해도 `~/.claude/plugins/...` 아래로 복사되지 않는데, README는 마치 별도 설치 폴더가 생기는 것처럼 안내하고 있었음(`known_marketplaces.json`의 `installLocation`이 원본 저장소 경로 그대로임을 직접 읽어 재확인 + 사용자의 실제 `cd` 실패 트랜스크립트로 이중 확인). README.md §설치 4번을 "이 원본 저장소 폴더에서 npm install, 별도 설치 폴더는 존재하지 않음"으로 정정. **잔여 미확인 사항 2건(사용자 세션에서만 확인 가능 — 이 세션에서는 재현 불가)**: ① `/reload-plugins` 시 나온 "1 error during load"가 이 플러그인 탓인지 — 이 플러그인의 manifest·hooks.json·commands·skill frontmatter는 독립 재검증 결과 전부 정상이라 가능성은 낮으나 확정 아님, `/plugin` 상세 확인 필요 ② `/sodam-design-kit:setup`의 진짜 성공 경로(shadcn 설치된 프로젝트에서 컴포넌트 시드) — 이번엔 플러그인 저장소 자체(shadcn 미설치)를 대상으로 실행돼 가드 경로만 확인됨, test11(이미 Next.js+Tailwind+shadcn+Button 스캐폴딩 완료)을 대상으로 재실행해야 확정. **P1 성공 기준 5·6번(새 세션 재현, 문서만 보고 재현)은 문서 정정 후 위 2건 확인이 완료돼야 최종 종결**
- **24차(2026-07-20, 23차 이후 문서 정리 라운드 — 새 실행 데이터 없이 재독으로 발견한 잔여 정리)**: ① 03_PHASES.md에 미체크로 남아있던 "핵심 도구 6개 공식 문서 링크 실측 확인"을 완료 처리 — 원래 목적(문서 링크 오류로 구현이 헛수고되는 것 방지)이 21~22차 실제 E2E 왕복으로 이미 달성됐다고 판단해 뒤늦은 링크 재검증(busywork)은 생략하고 사유만 기록. ② README.md §설치 4번(23차에 정정한 "복사 안 됨" 설명)에 **미래 리스크 각주 추가** — 04_PROJECT_SPEC.md 배포 방법에 이미 계획된 향후 GitHub 공개 전환 시 이 설명이 다시 틀리게 될 게 확실하므로, 같은 문서-현실 불일치가 재발하지 않도록 예방 각주 삽입. **의도적으로 하지 않은 것**: "1 error during load"의 정확한 원인을 이 세션에서 다른 78개 타 플러그인까지 뒤져 규명하는 것 — 04 "기설치 디자인 플러그인과의 역할 경계" 원칙상 이 킷의 스코프가 아니므로 배제(test11 세션에서 `/plugin` 확인만으로 충분). **P1 성공 기준 5·6번은 여전히 test11 세션의 사용자 확인 대기 — 변동 없음**
- **25차(2026-07-20, test11 실제 실행 로그 — P1 핵심 성공 기준 재현 + 진짜 결함 1건 추가 발견·수정)**: 사용자가 test11 세션의 전체 실행 로그를 공유. **핵심 성과: P1 성공 기준 5번("새 세션에서 marketplace 설치 후 동일하게 재현")이 실제로 달성됨** — `/plugin install`이 이번엔 성공(이전 세션에서 이미 설치됐던 것으로 확인), `/sodam-design-kit:setup`이 test11 안에서 정확히 실행돼 button 1개 시드, 이어서 `/sodam-design-kit:pipeline`으로 실제 Figma 커뮤니티 파일 노드(421:3524, Button)를 읽어 component-map에 매핑하고 실브라우저 검증까지 PASS로 왕복. 다만 이 과정에서 **⑤번째 진짜 결함**을 발견 — `commands/pipeline.md`가 "검증"과 "판정서 기록"을 별개 스크립트 단계처럼 서술했지만 `report-writer.mjs`에 CLI가 없어 실행 불가능했고(에이전트가 스크래치패드에 접착 스크립트를 직접 짜야 했던 것으로 실측 확인), 이는 01 §1 "고정 파이프라인" 핵심 가치를 위반하는 진짜 공백이었음. `verify-runner.mjs`에 `--target`/`--generatedFiles`/`--retryCount` CLI 옵션을 추가해 검증+판정서 기록을 명령 1개로 통합(하위 호환 유지)하고, PowerShell 실제 CLI 실행(fixture 대상 PASS 재현) + 신규 단위테스트 2건(48/48 전체 통과)으로 재검증 완료. pipeline.md 절차도 동기화. 부가 확인: "1 error during load"는 여전히 원인 미상(test11에서 `/plugin` 미확인)이지만 P1 성공 기준 달성 자체는 이와 무관하게 독립적으로 실증됨.

- **26차(2026-07-27, 문서 감사가 아닌 실행·재현·수정 — 게이트 무결성 라운드)**: 새 세션에서 PRD 9종(01~04·README·LIST·RESEARCH -01/-02/-03)과 코드 전체를 전수 재독하고 `npm test`를 실제로 돌린 결과, **문서가 사실과 어긋나 있음을 실측으로 확인**하고 진짜 결함 2건을 발견→재현→수정→재검증했다. ① **⑥번째 진짜 결함 — 진입점 조용한 실패(치명)**: `scripts/*.mjs` 4개 + `hooks/verify-gate.mjs`가 공유하던 "직접 실행 여부" 판정식이 `new URL(import.meta.url).pathname` 기반이라 **경로에 공백이나 한글이 있으면 `main()`이 실행되지 않고 무출력 + exit 0으로 끝나는 것**을 3케이스로 재현(`"My Projects"` false / `"한글경로"` false / ASCII true). `hooks/verify-gate.mjs`에서 이러면 판정 출력이 사라져 **완료 차단이 통째로 무력화**된다 — 02가 fail-closed로 못박은 결정이 바깥 진입점에서 뒤집히는 지점이자 01 §9 성공 기준 2번("게이트가 장식이 아님")과 정면 충돌. 원인은 pathname에 남는 퍼센트 인코딩(`%20`·`%ED%95%9C…`)과 디코딩된 `process.argv[1]`의 불일치. Node 내장 `fileURLToPath`로 5개 파일 전부 통일하고, **옛 판정식 사본을 한글+공백 경로에서 직접 실행해 stdout이 비는 것(차단 소멸)** vs 수정본이 `{"decision":"block",…}`을 내는 것을 대조 실증했다. 이 구간이 여태 안 걸린 이유는 기존 테스트가 전부 함수를 import해 직접 부르기만 하고 **"프로세스로 실행되는 구간"을 아무도 검증하지 않았기** 때문 — `tests/verify-gate.test.mjs`·`tests/setup-wizard.test.mjs`에 실제 프로덕션 파일을 공백·한글 경로에 복사해 실행하는 CLI 회귀 테스트를 각 1건 신설. ② **⑦번째 진짜 결함 — 날짜 시한폭탄 테스트**: 문서에 "50/50 통과"로 적혀 있었으나 실제로는 **49/50**이었다(`tests/report-writer.test.mjs`의 판정서 덮어쓰기 방지 테스트). `nextRunId(dir, date)`에는 날짜 주입구가 있는데 `writeReport()`에는 없어서 테스트가 실제 오늘 날짜에 묶였고 **작성 다음 날부터 깨져 있었다**. `writeReport({date})` 주입구를 열어 해소(실사용 호출부는 미전달 = 기존 동작 불변). 결과 **52/52 통과(실측)**. ③ 위 2건을 04 ALWAYS DO 2개 규칙으로 승격(진입점 판정·날짜 주입)하고 02에 결정 기록 추가, 03·README 한/영·md/html의 테스트 수치를 실제 값으로 동기화. ④ **부수 확인**: `D:\Test_Dev\test11`이 빈 폴더가 됐으나 실제 e2e 픽스처는 형제 폴더 `26y_07m_26d_SoDam-Design-Kit-Fixture`에 **온전히 살아 있음**(Next.js+shadcn button·`/broken`·프리뷰 라우트·`node_modules/next`·판정서 24건) — 진짜 공백은 픽스처 소실이 아니라 **경로가 저장소 어디에도 기록돼 있지 않았던 것**이라 `CHECKPOINT.md`에 고정 기록. P1 스코프·게이트 판정 기준·데이터 스키마 불변

- **27차(2026-07-27, 26차 이어서 — 실행·재현·수정, 이번 프로젝트 감사 전체에서 가장 심각한 결함 발견)**: 26차에서 실측 완료 확인한 e2e-selftest를 재점검하다가, `/broken` FAIL의 axe 위반 건수가 `critical 0 / serious 0`인데도 FAIL이 나는 모순을 발견했다. 원인을 끝까지 추적한 결과 — **⑧번째 진짜 결함(치명, 이번 감사 전체 최대)**: `verify-runner.mjs`의 `findAvailablePort()`가 포트 점유 여부를 "바인드 성공 시 비어있음"으로 판정했는데, **Windows는 다른 프로세스가 `0.0.0.0:<port>`로 이미 리슨 중이어도 `127.0.0.1:<port>`로의 별도 바인드를 허용**해버려서(SO_EXCLUSIVEADDRUSE 미설정 특성 — 순수 `node:net` 3줄 격리 스크립트로 별도 확인) 실제로 점유된 포트를 "비어있다"고 오판하고 있었다. 이 오판이 세션 도중 실제 조건 그대로 재현됨: 전혀 무관한 다른 프로젝트(BizPick)의 Next.js dev 서버가 `0.0.0.0:3000`을 점유 중이었는데, verify-runner가 그 포트를 "가용"으로 오판해 **그 다른 프로젝트의 화면을 검증하고도 target·devServer.port는 우리 것으로 표시된 그럴듯한 PASS 판정서(스크린샷 포함)를 실제로 만들어냈다**(`ibm_plex_sans_kr`이라는 fixture 소스에 없는 폰트 클래스가 응답 HTML에 박혀 있는 것을 실측 발견해 추적 시작 → `netstat`+`Win32_Process`로 PID·CommandLine 실측 확인 → 격리 스크립트로 Windows 바인드 허용 특이사항 재현 → 근본 수정). **이건 게이트가 판정을 잘못 내리는 정도가 아니라 "무엇을 검사했는지" 자체가 거짓일 수 있다는 뜻**이라 01 §9 "판정 근거" 성공 기준을 통째로 무너뜨리는, 이번 프로젝트 전체 감사에서 발견된 결함 중 가장 심각한 것으로 판단한다. 수정: 포트 점유 판정을 "바인드 시도"에서 "연결 시도"(연결 성공=점유, `ECONNREFUSED`/타임아웃=비어있음)로 뒤집었다 — 실제 클라이언트(Playwright·http)가 그 포트에 접속할 때 겪는 경로와 정확히 같아 Windows 특이사항과 무관하게 항상 정확하다. **무관한 BizPick 프로세스는 종료·조작하지 않음**(04 DO NOT·범위 원칙 — 우회로만 해결). BizPick 서버를 그대로 켜둔 채 재검증해 자동으로 `3002`로 우회하고 `/broken`이 이번엔 **진짜 axe critical 위반(1건)**으로 FAIL 나는 것을 실측 확인 — 이 킷의 핵심 차별점인 "axe 게이트가 실제로 위반을 잡아 차단한다"는 것이 이번 감사에서 **처음으로 E2E 프로세스 전체 왕복으로 증명됨**(그 전까지는 단위테스트의 `judge()` 직접 호출로만 증명돼 있었음). 회귀 테스트 신설(`tests/verify-runner.test.mjs` — 점유 주체를 `0.0.0.0`으로 만들어 옛 결함 조건 정확히 재현). 02·04에 결정 기록·규칙 승격, 03·README 한/영 md+html 테스트 수치(52→53) 동기화. **의도적으로 하지 않은 것**: BizPick 프로세스 종료(사용자 소유의 무관한 다른 프로젝트 상태 — 임의 종료는 범위 밖 파괴적 행동)

- **28차(2026-07-27, 26~27차 이어서 — 문서 감사가 아닌 실제 구현·실측 결과, M4·M5 완료)**: `pipeline.md`가 이미 서술만 해뒀던 "신규 컴포넌트 생성 경로"와 "실패 피드백 주입"에 필요한 재료를 실제로 구현·실측 완료했다. **M4(신규 컴포넌트 생성 경로)**: `pipeline-codegen.mjs`에 `findHardcodedStyleViolations()`(04 DO NOT "하드코딩 hex·px 금지"를 코드로 처음 강제 — 대괄호 전체가 순수 hex/px일 때만 위반 판정, 실제 `button.tsx`의 `rounded-[min(var(--radius-md),10px)]` 같은 CSS 변수 참조 패턴은 오탐 없음을 실측 확인)와 `registerNewComponent()`(경로 정규화·중복 등록 차단·component-map 등록·프리뷰 라우트 생성)를 신설, 단위테스트 8건 추가(61/61), 픽스처에서 실제 CLI로 합성 Badge 컴포넌트를 등록→검증 왕복 PASS 실측(드라이런 후 픽스처는 커밋 기준선으로 원복). **M5(FAIL 피드백 재료 확보)**: `verify-runner.mjs`가 axe-core 위반 상세(규칙 id·설명·대상 선택자)를 세기만 하고 그 자리에서 버리고 있던 공백을 발견 — `summarizeAxeViolations()` 신설(PASS/FAIL 판정 로직 `judge()`는 무변경), `report-writer.mjs`가 판정서에 "위반 상세" 섹션을 자동 기록하도록 확장, `pipeline.md` 재시도 절차에 "이 상세를 다음 생성 시도에 반영하라" 지시 추가. 단위테스트 5건 추가(66/66), 픽스처 `/broken`에 실제 axe-core 설명 문자열이 판정서에 그대로 찍히는 것을 실측 확인. **범위를 의도적으로 "자동 재생성"이 아니라 "재생성에 쓸 재료를 기록하는 것"으로 좁혔다** — 무엇을 어떻게 고칠지 스스로 판단하는 것은 AI 판단 영역이라 `pipeline.md`의 절차 지시로 에이전트에게 위임. 02·03·CHECKPOINT.md에 완료 기록. **P1 스코프·게이트 판정 기준 불변**

- **29차(2026-07-27, 28차 이어서 — Phase 2 착수 검토 + 감사 이력 정합성 복구)**: 사용자의 "다음 Phase 작업 방향" 요청을 계기로 Phase 2 착수 여부를 검토했다. `03_PHASES.md`가 명시한 Phase 2 전제조건("Phase 1이 새 세션 설치 테스트까지 통과한 상태")이 M1 미완료(사용자 전용 관문)로 아직 미충족이라 **Phase 2 코드 착수는 보류**하기로 결정하고, 코드·킷 파일 변경이 전혀 없는 웹 리서치 3건만 수행했다(Figma MCP 호출 없이 수행 — 무료 월 6회 예산 미소모). ① **OFL 폰트 라이선스**: PRD가 이미 계획한 "OFL.txt 동반 보존" 방식이 정확함을 공식 OFL-FAQ로 확인 — 설계 변경 불필요. ② **Figma Variables API 요금제**: Variables GET/POST가 기존에 실측 확인된 "월 6회" 제한(읽기 도구 전용)과 다른 Tier(분당 5~10회)로 분류되는 것으로 보임(가능성, 미확정 — Phase 2 착수 시 소량 실호출로 재확인 필요. 사실이면 P2 최대 리스크였던 예산 부족이 완화될 가능성). ③ **shadcn Registry MCP — PRD 정보가 낡았음을 실제로 확인**: PRD는 `npx shadcn@canary registry:mcp`(canary 전용)로 적어뒀으나, 공식 문서(ui.shadcn.com/docs/mcp) 확인 결과 이미 정식 `shadcn@latest mcp`로 전환돼 있었다(Claude Code 전용 설치 명령 `pnpm dlx shadcn@latest mcp init --client claude`까지 제공) — `03_PHASES.md`의 해당 항목을 정정. 이 결정과 리서치 결과는 `CHECKPOINT.md` "Phase 2 착수 검토" 절에 우선 기록했다. 이어서 **이 감사 이력 로그 자신이 27차 이후 갱신되지 않아 M4·M5·이번 결정이 반영되지 않은 공백을 재독으로 발견** — 28차·29차 항목 신설로 해소했다(바로 이 항목). **P1 스코프·게이트 판정 기준·Phase 2 착수 여부 전부 불변 — 코드 변경 없음, 문서만.**

- **30차(2026-07-27, 29차 직후 — 실행·재현·수정, 진짜 결함 2건 추가 발견. 코드는 그날 커밋됐으나 이 이력엔 그동안 누락돼 있던 것을 2026-08-04에 재독으로 발견해 소급 기록)**: 29차 문서 정합성 복구 직후 이어진 검증 라운드에서 `npm run selftest`를 실제로 실행하다가 진짜 결함 2건을 추가로 재현·수정했다. ① **selftest 스크립트가 존재하지 않는 옵션을 가리키고 있었음**: `package.json`의 `selftest` 스크립트가 `verify-runner.mjs --selftest`를 실행하도록 되어 있었으나 그런 옵션은 애초에 구현된 적이 없어(04 "테스트 방법"의 정본은 `e2e-selftest.mjs`) 실행하면 사용법 안내만 찍고 끝났다 — `package.json`을 실제 파일(`scripts/e2e-selftest.mjs`)을 가리키도록 정정(커밋 `b547ec1`). ② **`verify-runner.mjs`를 setup 없이 `--target`만으로 직접 실행하는 경로(기존 테스트가 의도적으로 허용·검증하는 정상 사용법)에서 스크린샷 `.gitignore` 등록이 빠져 있었음**: 이 등록 로직은 지금까지 `setup-wizard.mjs`에만 있었는데, setup을 거치지 않고 `.design-kit`가 새로 생기는 이 경로에서는 등록이 전혀 일어나지 않아 22차에서 이미 한 번 고친 것과 같은 결함(스크린샷 git 노출 위험)이 이 경로로는 여전히 재현됨을 실측 확인 — `report-writer.mjs`의 `writeReport()`에도 동일한 `ensureScreenshotsGitignored()`를 추가해 해소(커밋 `ae5879a`). 두 수정 모두 기존 66개 테스트에 영향 없이 전부 통과. 같은 라운드에서 README 4파일(md/html 한/영)의 테스트 개수·구현 상태 표기를 66개 기준으로 동기화했다(커밋 `479b96c`). **이 항목 자체가 기록된 경위**: 코드·README 수정은 그날 바로 커밋됐지만 이 감사 이력에는 반영되지 않은 채로 일주일 가까이 방치돼 있었다 — `CHECKPOINT.md`가 스스로 "이게 세 번째로 같은 패턴(코드는 고쳤는데 이력엔 없음)이 쌓이는 것"이라 경고해뒀던 그 공백이 실제로 발생한 사례다. 2026-08-04 재감사에서 발견해 소급 기록. **P1 스코프·게이트 판정 기준 불변.**

- **31차(2026-08-04, 새 세션 재감사 — PRD 9종 전수 재독 + 실측 검증, M1 실행 준비 완결)**: PRD 9종(01~04·README·LIST·RESEARCH -01/-03)을 전수 재독하고 `npm test`(66/66)·`npm audit`(0건)·`git fetch`+`status`(origin과 완전 동기화)·GitHub 저장소 상태(`gh repo view`)·픽스처 폴더 실재 여부를 전부 직접 재확인했다. 그 결과 **`CHECKPOINT.md`가 실제 상태와 어긋나 있는 지점 3건**을 실측으로 발견했다: ① 테스트 개수가 "53/53"으로 낡아 있었음(실제 66/66) ② "로컬 커밋이 origin보다 3개 앞서 있고 아직 push 안 됨"으로 적혀 있었으나 실제로는 완전 동기화된 상태였음(30차의 커밋들이 이미 push까지 끝나 있었음) ③ **가장 심각한 것 — M1 실행서가 픽스처의 "현재 최대 판정서 번호"를 `2026-07-20-024`로 하드코딩해두고 있었으나, 실제로는 그 뒤 여러 차례의 예행연습으로 `.design-kit/runs/`에 `2026-07-27-010`까지 물리적으로 쌓여 있었다**(실측 확인). 원인을 추적한 결과 "드라이런 후 픽스처는 커밋 기준선으로 원복"이라는 이전 기록 자체가 틀린 전제였다 — `runs/`·`reports/`는 `.gitignore` 대상이라 애초에 git 커밋 범위 밖이고, 따라서 git으로 "원복"될 수 없다(디스크에 파일이 그대로 남는다). 이 상태로 사용자가 M1을 실행했다면 "기존 판정서가 안 지워졌는지" 확인 기준 자체가 틀려 있어 정상적인 결과도 오판될 위험이 있었다. **수정 방식은 단순히 숫자를 최신값으로 갱신하는 게 아니라 구조를 바꿨다**: M1 실행서에 특정 파일명을 다시 고정하는 대신, 실행 직전에 `Get-ChildItem`으로 그 순간의 실제 최댓값을 직접 확인하도록 절차를 변경했다(재발 방지 — 하드코딩된 스냅샷은 시간이 지나면 반드시 낡는다). `CHECKPOINT.md`의 "다음 작업" 우선순위 절도 1~3순위가 이미 완료된 상태에 맞게 재정리했다. 이어서 §9 성공 기준 6번("최소 문서 세트가 존재하고, 새 세션 설치 재현이 **그 문서만 보고** 가능")이 M1 대상으로는 충족되지 않고 있음을 확인했다 — M1의 구체 실행 명령이 `CHECKPOINT.md`(gitignore 대상, git에 없음)에만 있고 README에는 없었다. README.md·README.en.md §4(빠른 시작)에 M1 실행 절차(대상 프로젝트·명령 두 줄·합격 판정 기준)를 왕초보 눈높이로 이식해 해소했다(아래 참조). **P1 스코프·게이트 판정 기준·Phase 2 착수 여부 전부 불변 — 코드 변경 없음, 문서 정합성 복구만.**

- **32차(2026-08-04, 31차 직후 — 사용자의 실제 M1 새 세션 라이브 실행 + 진짜 결함 발견·수정·재검증)**: 사용자가 31차에서 정리된 문서를 보고 실제로 새 Claude Code 세션을 열어 `/sodam-design-kit:pipeline`을 픽스처 대상으로 실행했다. 결과 자체는 PASS(렌더 OK·콘솔 에러 0·axe critical/serious 0·기존 판정서 10건 전부 보존)였지만, **판정서 상세를 파일 시스템과 직접 대조하는 검증 과정에서 ⑨번째 진짜 결함을 발견**했다: 판정서가 `reports\screenshots\pipeline-421-3078\360.png` 등 3장이 존재한다고 정확히 기록했지만, 그 경로는 프로젝트 어디에도 없었다. 전체 디스크 추적 끝에 스크린샷 3장이 **`C:\Users\PC\reports\screenshots\pipeline-421-3078\`**(프로젝트 밖, 사용자 홈 폴더)에 실제로 저장돼 있음을 확인 — 파일 자체는 진짜(올바른 크기의 PNG)였지만 위치가 완전히 틀렸다. 원인 추적: `verify-runner.mjs`가 `--screenshotDir`를 받은 그대로(상대경로 포함) 써서, 새 세션의 에이전트가 Git Bash 경로 오염 문제(위 04 트러블슈팅)로 PowerShell로 도구를 바꿔 재시도하는 과정에 실제 cwd가 프로젝트가 아니라 Claude Code 실행 위치였던 것으로 추정된다. **이건 27차 포트 오판과 같은 등급("판정 근거 자체가 거짓일 수 있다")의 문제이면서, 재현 조건은 더 넓다** — 포트 오판은 특정 상황에서만 발생하지만 이건 에이전트가 절대경로를 안 쓰면 사실상 항상 재현된다. 수정: `--screenshotDir`가 상대경로면 `--project` 기준 절대경로로 정규화(`path.isAbsolute` 체크 후 `path.join(path.resolve(projectDir), screenshotDir)`), 04 DO NOT 원칙("경로는 프로젝트 루트 하위로 정규화")이 이 인자에만 빠져 있던 공백을 메움. 회귀 테스트 신설(다른 cwd로 위장해 상대경로 --screenshotDir 호출 → 프로젝트 안에 저장되고 cwd 기준 위치엔 안 생기는 것 확인, `npm test` 66→67 전부 통과) + 다른 cwd(`C:\Users\PC`)에서 픽스처를 대상으로 실제 CLI 재현 테스트(PowerShell, 원래 사고와 동일 조건)해 수정 후엔 프로젝트 안(`Fixture\reports\screenshots\...`)에 정확히 저장됨을 실측 확인. 잘못 생성됐던 두 위치(`C:\Users\PC\reports\`, 드라이런 검증용 `Fixture\reports\`)의 스크린샷 파일은 개별 삭제로 정리(폴더 단위 삭제는 로컬 안전장치가 차단해 빈 폴더 껍데기만 잔존 — 데이터 없음, git 미추적 확인). **원래 M1 라이브 세션의 판정 기록(`2026-08-04-001`)은 진짜 실행 이력이라 보존, 수정 검증용 드라이런 기록(`2026-08-04-002`)만 정리.** 02·04에 결정 기록·규칙 승격. **M1은 이번에도 공식 종료로 선언하지 않는다** — 이번 실행은 수정 전 코드로 이루어졌고, 스크린샷 경로 결함이 있는 상태였다. 수정된 코드로 새 세션에서 다시 한번 M1을 실행해야 §9 성공 기준이 정직하게 충족된다. **P1 스코프·게이트 판정 기준 불변.**

- **33차(2026-08-04, 32차 직후 — 사용자의 두 번째 실제 M1 새 세션 시도, 진짜 결함 한 겹 더 발견·수정)**: 32차 수정을 반영한 코드로 사용자가 새 세션을 다시 열어 M1을 재시도했다(`runId 2026-08-04-005`, verdict PASS). 이번엔 스크린샷이 프로젝트 밖으로 탈출하진 않았지만, 판정서에 `../reports/screenshots/360.png`처럼 `..`로 시작하는 경로가 적힌 것을 발견해 추적한 결과, `.design-kit/` **밖**(프로젝트 루트의 형제 폴더 `Fixture\reports\screenshots\`)에 저장돼 있었다 — `.gitignore`가 `.design-kit/reports/screenshots/`만 등록해뒀으므로 이 위치는 git에 그대로 추적되는 상태(`git status`에 `?? reports/`로 실측 확인)였다. 서로 다른 두 라이브 세션이 똑같이 "상대경로에 `.design-kit`를 안 붙이는" 패턴을 보였으므로, `verify-runner.mjs`의 기준점을 프로젝트 루트에서 `.design-kit/` 자체로 올렸다(이미 `.design-kit`로 시작하면 중복 접두 방지). 원래 사고와 동일한 상대경로로 다른 cwd에서 재현 테스트해 이번엔 `.design-kit\reports\screenshots\`에 정확히 저장됨을 실측 확인, 회귀 테스트 2건 추가(66→68). 잘못 놓인 스크린샷 파일은 정리했고, 실제 판정 기록(`2026-08-04-005`)은 역사적 사실이라 보존했다. 상세는 `.PRD/02_DATA_MODEL.md`·`04_PROJECT_SPEC.md` 결정 기록. **P1은 이번에도 공식 종료로 선언하지 않는다** — 이 실행 자체는 스크린샷 위치 결함이 있는 코드로 이루어졌으므로, 최종 수정된 코드로 세 번째 새 세션 확인이 필요하다. **P1 스코프·게이트 판정 기준 불변.**

- **34차(2026-08-04, 33차 직후 — 사용자의 세 번째 실제 M1 새 세션 시도, 이번엔 스크립트가 아니라 절차 문서 층의 결함 발견·수정)**: 32·33차 수정을 반영한 코드로 사용자가 세 번째로 새 세션을 열어 M1을 시도했다. 이번엔 스크린샷 경로 문제가 아니라 더 앞 단계에서 멈췄다 — component-map.json에 이미 노드 421:3078 매핑이 있는데도, 에이전트가 `commands/pipeline.md` 1번 절차("Figma 읽기")를 문자 그대로 따라가 Figma 파일 URL을 요구하며 진행을 멈췄다. 원인 추적 결과 `pipeline.md`의 "재사용 경로" 절차 자체가 이름과 다르게 동작하고 있었다 — 1번 단계에 "매핑이 이미 있으면 건너뛴다"는 분기가 전혀 없었다(상세는 `02_DATA_MODEL.md` 결정 기록). `pipeline-codegen.mjs` 스크립트 자체는 그동안 여러 번(2026-07-20·07-27) 실측 확인된 대로 정확히 동작하지만, 에이전트가 그 스크립트에 도달하기도 전에 절차 문서 자체의 공백 때문에 멈춰버리는 상위 레벨 결함이었다. `pipeline.md`에 "0번" 단계를 신설해 component-map.json을 먼저 확인하고 매핑이 있으면 1·2번을 건너뛰도록 명시했다. 이건 절차 문서 수정이라 단위테스트로 회귀 검증이 불가능하다 — 네 번째 새 세션 M1 시도가 유일한 실증 방법이다. **P1은 이번에도 공식 종료로 선언하지 않는다** — 네 번째 시도가 필요하다.

- **35차(2026-08-04, 34차 직후 — 사용자의 네 번째 실제 M1 새 세션 시도, 최초의 완전한 성공 + AI 독립 재검증 — 🎉 P1 공식 완료)**: 34차에서 `pipeline.md`에 신설한 0번 단계(매핑 있으면 Figma 건너뛰기)를 반영한 코드로 사용자가 네 번째로 새 세션을 열어 M1을 시도했다. 트랜스크립트만으로 "PASS"를 받아들이지 않고, 이전 세 차례와 동일하게 AI가 독립적으로 재검증했다: ① `.design-kit/reports/2026-08-04-009.md` 직접 열람 — verdict PASS, 렌더 OK, 콘솔 에러 0, axe critical 0/serious 0(moderate 1건은 `page-has-heading-one` — 컴포넌트 단독 프리뷰 페이지의 구조적 특성이라 게이트 기준 밖, 판정서에 기록됨). ② 스크린샷 3장(`360.png`·`768.png`·`1440.png`)이 판정서가 적은 경로 그대로 `.design-kit/reports/screenshots/pipeline-421-3078/`에 **실제로 존재**함을 `find` 명령으로 확인(1·2차 때 반복됐던 프로젝트 밖·`.design-kit` 밖 탈출 없음 — 32·33차 수정이 실제로 유효함을 최종 실증). ③ 기존 판정서 `2026-07-19-001`부터 `2026-08-04-008`까지(총 43건) 전부 보존, 덮어쓰기 없음. ④ `git status`·파일시스템 스캔으로 임시 접착 스크립트 흔적 0건. ⑤ 3차 시도를 막았던 "Figma URL 요구" 없이 "재사용 경로(Figma 호출 0회)"로 곧장 진행됨을 트랜스크립트와 실행 결과(component-map.json 재조회만, 신규 Figma 호출 없음) 양쪽에서 확인 — 34차 수정이 실제 새 세션에서 의도대로 작동함이 최초로 실증됐다. **결론: `.PRD/01_PRD.md` §9 성공 기준 6개 전부가 실제 증거로 충족됐다** — 각 기준에 이 실행 결과를 근거로 체크 표시했다. 2026-08-04 하루 동안의 4차례 M1 시도가 순서대로 드러낸 결함(①스크린샷 프로젝트 밖 탈출 →②`.design-kit` 밖 탈출 →③pipeline.md Figma-스킵 절차 공백)이 전부 실제로 고쳐졌고, 그 수정들을 전부 반영한 이 4차 시도가 처음으로 아무 결함 없이 끝까지 통과했다. **P1(MVP)을 공식 완료로 선언한다.** `03_PHASES.md` Phase 로드맵·"진짜 킷 체크리스트"를 완료로 갱신했다. 남은 것은 P1 범위 밖의 낮은 우선순위 항목뿐이다: M6(README 최종 다듬기, 이미 초안은 충분히 상세함)·M2(무관 가능성 높은 플러그인 로드 경고 원인, 미확정이나 P1과 무관)·moderate 등급 h1 접근성 지적(선택적 개선, 게이트 통과에 영향 없음). **Phase 2 착수 전제조건(`03_PHASES.md`: "Phase 1이 새 세션 설치 테스트까지 통과한 상태")이 이제 충족되어, 사용자가 원하면 Phase 2 논의를 시작할 수 있다.**

- **36차(2026-08-09, Phase 2 첫 증분 — 문서 감사가 아닌 실제 구현·단위테스트·실제 프로세스 동시 실행 실증)**: P1 완료 후 처음으로 Phase 2 코드에 착수했다. 다만 통째로 만들지 않고 리스크가 가장 큰 기능(open 대시보드)의 전제조건인 `.design-kit/.lock` 실행 잠금(04 연결/동기화 스펙 2번)만 독립적으로 먼저 구현했다 — 대시보드는 이 프로젝트가 반복해온 "문서엔 규칙이 있는데 강제하는 코드가 없다" 패턴(22·26·28·34차와 동일 구조)에 가장 가깝게 서 있는 기능이라 판단해, 쪼개서 검증하는 전략을 택했다. `scripts/execution-lock.mjs`를 신설(`acquireLock`/`releaseLock`, PID 생존 확인 기반 stale 판정, 손상 파일 fail-closed)하고 `verify-runner.mjs`의 `main()`에 배선했다. 구현 중 실제 결함 1건을 발견·수정했다 — 잠금 파일을 쓰기 전에 `.design-kit/` 폴더 존재를 보장하지 않아, setup을 거치지 않고 `--project`만으로 직접 실행하는 기존 지원 경로(report-writer.mjs의 writeReport()와 동일 전제)에서 `ENOENT`로 조용히 죽어 CLI stdout이 빈 채로 끝나는 것을 회귀 테스트 3건 실행 중 실측 발견, `mkdir(designKitDir, {recursive:true})`로 즉시 수정. 단위테스트 11건 신설(`npm test` 68→79, 전부 통과) + **실제 프로세스 2개로 동시 실행**: 픽스처 대상 CLI 2개를 거의 동시에 실행해 1번째 PASS 완료·2번째는 `[lock] 다른 실행이 이미 진행 중입니다 (PID ...)`로 정확히 거부, 거부된 시도가 `runs/`에 파일을 전혀 남기지 않음(순번 오염 없음, 48건→50건=+1만 증가)까지 실측 확인. `npm audit` 0건 유지. **범위를 의도적으로 대시보드 자체(읽기 화면·트리거 버튼)와 분리했다** — 그건 다음 증분이다. 상세는 `.PRD/02_DATA_MODEL.md`·`04_PROJECT_SPEC.md` 결정 기록.

- **37차(2026-08-09, 36차 직후 — 대시보드 보안 골격, `.lock`과 같은 쪼개기 전략의 다음 단계)**: `.lock` 다음으로 대시보드 자체를 통째로 만들지 않고, 그중 보안 primitive만(`/health` 라우트 1개, 데이터 0건) 먼저 독립 구현했다. 착수 전 O-Brain의 실제 `server.mjs`(D:\...\26y_06m_21d_SoDam_O-Brain\app\src\server.mjs)를 직접 읽고 이식했다 — "O-Brain 패턴 이식"이라는 문서 지시를 처음으로 문자 그대로 지켰다(그 전까지는 실제로 읽은 적 없이 설명만 반영하는 게 됐을 뻔함). 이식 중 하나 개선했다: O-Brain의 토큰 비교는 `!==` 직접 비교였는데, 이 킷은 `crypto.timingSafeEqual`로 강화(Node 내장이라 신규 의존성 없음). 프레임워크도 O-Brain의 express 대신 Node 내장 `http`만 사용(이 킷엔 웹 프레임워크가 아예 없어 골격 하나 때문에 새 의존성을 들이지 않음 — 공급망 최소화). 단위테스트 17건 신설(79→96) + 실제 CLI로 픽스처 대상 서버를 띄워 `Get-NetTCPConnection`으로 127.0.0.1 단독 바인딩 확인·올바른 토큰 200/틀린 토큰 403/토큰 없음 403/CSP 헤더 응답 포함까지 실측 확인. `npm audit` 0건 유지. **범위를 의도적으로 좁혔다** — 판정서·스크린샷을 실제로 보여주는 것(2b)과 재검증 트리거(2c)는 다음 증분. 상세는 `.PRD/02_DATA_MODEL.md`·`04_PROJECT_SPEC.md` 결정 기록.

- **38차(2026-08-09, 37차 직후 — 2b를 더 쪼갠 첫 조각: 데이터 API + 경로 조작 방어)**: 2b를 "데이터 API"와 "실제 화면"으로 재분할한 뒤 그 첫 조각만 구현했다. `/api/runs`·`/api/reports/:runId`·`/api/screenshots/*` 3개 GET 라우트 신설 — 04_PROJECT_SPEC.md DO NOT가 이름으로 지목한 "스크린샷 서빙" 경로 조작 위험을 runId 정규식 선검증 + resolve/startsWith + 확장자 화이트리스트 3중으로 방어했다. XSS 위험이 실제로 생기는 HTML 렌더 단계(2b-2)는 의도적으로 미뤘다 — JSON 단계는 브라우저가 실행할 마크업이 없어 위험이 다르다고 판단. 단위테스트 13건 신설(96→109) + **실제 픽스처(실행 이력 50건 보유) 대상 실측**: `/api/runs` 200(50건)·`/api/reports/<실ID>` 200(실제 판정서 내용)·`/api/screenshots/pipeline-421-3078/360.png` 200(진짜 PNG 3469바이트)까지 정상 조회되고, `..%2f..%2f` 경로 조작 시도는 두 라우트 전부 400으로 차단됨을 확인. `npm audit` 0건. 상세는 `.PRD/02_DATA_MODEL.md` 결정 기록.

- **39차(2026-08-09, 38차 직후 — 2b-2: 실제 화면 + XSS 방어 실측, Phase 2 대시보드 4단계 중 3단계 완료)**: 2b를 쪼갠 두 번째 조각을 구현했다. `scripts/dashboard-web/`(index.html+dashboard.js) 신설, `GET /`·`GET /dashboard.js`를 토큰 검사 밖으로(셸은 공개, 데이터는 계속 보호). **착수 전 설계 검토에서 실제로 터졌을 결함 2건을 코드 짜기 전에 미리 잡았다**: 스크린샷 `<img>`가 커스텀 헤더를 못 보내는 문제(→ fetch+Blob URL로 해결) / 인라인 `<script>`가 자기 CSP에 막히는 문제(→ 외부 js 분리로 해결). `dashboard.js`는 innerHTML을 전혀 안 쓰고 textContent만 쓴다 — 이걸 "코드 리뷰 규칙"이 아니라 **실제 헤드리스 브라우저(Playwright, 기존 의존성)로 악성 payload를 진짜 렌더해서** `window.__xssFired`가 끝까지 `undefined`임을 실측 증명했다. 단위테스트 109→116(XSS 실측 포함). 실제 픽스처 대상 `GET /` 토큰 없이 200 + 토큰 정확히 주입 확인. `npm audit` 0건. 상세는 `.PRD/02_DATA_MODEL.md` 결정 기록. 남은 건 2c(재검증 트리거)뿐.

- **40차(2026-08-09, 39차 직후 — 2c: 재검증 트리거, 🎉 Phase 2 대시보드 4단계 전부 완료)**: 착수 전 설계 검토에서 실제 구현을 막았을 문제 2가지를 미리 잡았다. ① `PipelineRun`에 검증 대상 `route`가 애초에 저장된 적이 없어(스키마 공백) 재검증이 "어느 화면을 다시 볼지" 알 방법이 없었다 → `report-writer.mjs`·`verify-runner.mjs`에 배선해 이제 저장됨(구버전 판정서는 명확히 재검증 거부, 경로 추측 없음). ② 지난 세션에 스스로 남긴 "대시보드 GET 전용 원칙" 메모를 재검토해 **틀렸음을 확인** — 02가 금지한 건 "직접 쓰기"이지 "POST 사용"이 아니었다. `POST /api/reverify/:runId` 하나만 예외로 허용(트리거는 코어 엔진 재사용, 새 판정 로직 없음 — main()을 안 건드리고 이미 export된 함수만 재사용). `execution-lock.mjs`에 `statusCode: 409` 추가해 화면이 친절한 문구로 번역 가능하게 함. 단위테스트 6건(거부 경로) + **실제 픽스처 대상 실측 4건**: 토큰 없는 요청 403(위조 요청 방어) / 허용 안 된 Origin+정상 토큰 403 / **진짜 동시 POST 2건 — 하나 200(PASS)·하나 409(거부, 파일 0개 생성)** / 기존 판정서 50건 전부 보존(52건=50+시드+성공, 산술 정합). `npm audit` 0건. **Phase 2 대시보드(`.lock`→2a 보안골격→2b-1 데이터API→2b-2 실제화면→2c 재검증트리거) 4단계 전부 완료.** 상세는 `.PRD/02_DATA_MODEL.md` 결정 기록.

- **41차(2026-08-09, 40차 직후 — 독립 검증 라운드: 스크린샷 gitignore 누락 결함 발견·수정)**: 이번 세션 전체(`.lock`·대시보드 4단계) 구현을 "문서 감사"가 아니라 실제 테스트·재현으로 처음부터 다시 검증했다. `npm test` 123/123·`npm audit` 0건·E2E 셀프테스트 전체 사이클·CLI↔대시보드 교차 `.lock` 동시성(신규 실측)·극단 입력(5000자 runId·유니코드·잘못된 URL 인코딩) 전부 서버 크래시 없이 400 확인. **실제 Playwright 브라우저로 실 픽스처를 열어보다가 진짜 결함 1건 발견**: `--screenshotDir`에 `reports/` 접두가 빠진 상대경로를 쓰면 스크린샷이 `.design-kit/screenshots/`에 저장되는데(정규화 규칙상 정상) 이 위치가 `.gitignore`에 등록돼 있지 않아 git 추적 노출 위험이 있었다(22차·32차·33차와 같은 계열의 결함). `ensureScreenshotsGitignored()`(report-writer.mjs·setup-wizard.mjs)가 정본 위치와 이 위치 둘 다 등록하도록 수정, 회귀 테스트 1건(122→123), 실제 픽스처 자가 치유 적용해 노출 해소 확인. `readScreenshotFile()`의 서빙 경로는 의도적으로 넓히지 않음(경로 조작 방어 약화 방지). 상세는 `.PRD/02_DATA_MODEL.md` 결정 기록.

- **42차(2026-08-09, 41차 직후 — `/sodam-design-kit:open` 진입점 신설, 대시보드 "엔진은 있으나 문이 없던" 공백 해소)**: 41차 검증 도중 PRD(01 §3·03)가 명시한 대시보드 진입점 슬래시 명령이 실제로는 어디에도 없다는 걸 발견했다(`commands/`엔 setup.md·pipeline.md뿐, `startDashboardServer()` 엔진은 이미 완성돼 있었음). `commands/open.md` 신설 + `scripts/dashboard-server.mjs`에 백그라운드 기동(`spawn(...,{detached:true}).unref()`)·재사용(이미 떠 있으면 재시작 안 함)·`--stop` 종료·브라우저 자동 오픈(OS별 바이너리 인자 배열 호출, 셸 문자열 조합 없음)을 추가했다. 새 상태 파일 `.design-kit/.dashboard.json`(pid·port·url만, 토큰 없음) 신설. 개발 중 테스트로 실제 버그 1건 발견·즉시 수정: stale 상태 파일을 지우지 않고 새 자식을 기다리면 대기 로직이 옛 파일을 "새 자식이 씀"으로 착각해 옛 정보를 반환하던 결함. 단위테스트 12건 신설(123→134, 재사용/새시작/stale회수/대기초과/종료 3경로/브라우저 호출형태 — isAlive·kill·spawnFn 전부 주입 가능해 테스트가 실제 시스템 프로세스를 안 건드림) + **실제 분리 프로세스 왕복 테스트 1건**(가짜 함수 없이 진짜 자식 프로세스로 진짜 HTTP 200 받고 진짜로 종료) + **실제 픽스처 대상 수동 실측**: 기동→HTTP 200→Chrome 실제 오픈(프로세스 생성 시각 대조로 확인)→재실행 시 재사용→`--stop`으로 실제 PID 종료→포트 응답 중단 전 구간 확인. `npm audit` 0건. README 4파일(md/html × ko/en) 명령어 표 동기화. 상세는 `.PRD/02_DATA_MODEL.md` 결정 기록.

- **43차(2026-08-09, 42차 직후 — axe moderate `page-has-heading-one` 해소)**: `preview-route.mjs`가 생성하는 프리뷰 페이지에 `<h1>`이 없어 axe moderate 위반(`page-has-heading-one`)이 판정서에 계속 남아있었다(`2026-08-04-009` 등). critical/serious만 게이트 기준이라 PASS 자체는 항상 정상이었지만, 검증을 표방하는 킷 자신의 산출물이 접근성 지적을 남기는 건 정체성과 어긋난다고 판단해 해소했다. sr-only 기법(`position:absolute`+`clip`, `display:none` 아님 — axe와 스크린리더 둘 다 "존재함"으로 인식)으로 시각적으론 안 보이게 넣어 01 §5의 "프리뷰 라우트는 미니멀이 기능 요건"과 충돌하지 않게 했다. 단위테스트 1건 신설(134→135) + **실제 픽스처 재생성 후 진짜 axe-core 실행으로 실측**: `axeCounts.moderate` 1→0, `axeViolations: []`, PASS 유지 확인.

## 핵심 결정 이력 (인터뷰 확정)

- 결과물: Claude Code 플러그인 (독립 서비스 아님, Codex는 AGENTS.md로 대응) + P2 open 대시보드·P3 MCP 래퍼로 Claude Desktop 확장 (2026-07-19 사용자 결정 — GPT Desktop은 조건부 백로그)
- MVP: 파이프라인 명령 + 검증 게이트 (Storybook·토큰은 Phase 2)
- 비용: 무료·오픈소스 도구만. 베타 반응 좋으면 구독 서비스 전환 고려 (Phase 3, 사용자 결정)
- 로그인: 불필요
